export const adjustCellCount = cellCount => ({
  type: 'CELL_COUNT',
  cellCount
})

export const adjustGliderCount = gliderCount => ({
  type: 'GLIDER_COUNT',
  gliderCount
})

export const adjustBlinkerCount = blinkerCount => ({
  type: 'BLINKER_COUNT',
  blinkerCount
})